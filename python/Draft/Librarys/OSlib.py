import os


os.system("cls")                    # Commend for the termenal.
os.rmdir("Lib")                     # Remove dirctory.
os.mkdir("Lib")                     # create a new dirctory.
os.chdir("Lib")                     # Change dirctory => "cd Lib"       | This commemd dont change the path in the terminal.
os.cpu_count()                      # Return the number of CPUs (cores).|On my case => 8
